#PBS -l nodes=1:ppn=2
#PBS -l mem=4gb
#PBS -l walltime=3:59:00
#PBS -j oe
#PBS -N radius
#PBS -l feature=intel14
#PBS -o /mnt/scratch/goldma72/radius_${PBS_JOBID}.tmp

# Required by C++11
module load GNU/4.8.2

cd ${PBS_O_WORKDIR}

if [ ! -f radius/finished.txt ]
then
  	qsub radius.sub
    # Runs the correct job
    timeout -s SIGINT 14360 python radius_scheduler.py ${PBS_JOBID}
    qstat -f ${PBS_JOBID}
fi
